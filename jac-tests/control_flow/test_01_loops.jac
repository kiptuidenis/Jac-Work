# Test 01: Conditionals and Loops (Broken)

with entry {
    # 1. C-Style Loop Fracture (Missing keywords 'to' and 'by')
    for i = 0 to i  < 10 by i += 1 {
        print(i);
    } # Error: Expected 'to'
    
    # 2. Python-style IF (Missing braces)
    # if True
    #     print("Failure"); 
}
